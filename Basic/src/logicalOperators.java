public class logicalOperators {
    static void main() {
        boolean is_student = true;
        boolean is_teacher = false;
        System.out.println(is_student && is_teacher);
        System.out.println(is_student || is_teacher);
        System.out.println(!is_student);

    }
}
